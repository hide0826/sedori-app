@echo off
REM ラッパーバッチファイル（確実にウィンドウを開いたままにする）

REM 引数をそのまま渡す
if "%~1"=="" (
    echo ============================================================
    echo Google Cloud Vision API 動作確認
    echo ============================================================
    echo.
    echo 使用方法: このファイルに画像ファイルをドラッグ^&ドロップするか、
    echo またはコマンドラインで画像ファイルパスを指定してください。
    echo.
    echo 例: test_gcv_ocr_wrapper.bat "D:\receipts\receipt.jpg"
    echo.
    pause
    exit /b 1
)

REM メインのバッチファイルを実行（cmd /k で開いたままにする）
cmd /k "cd /d %~dp0.. && python scripts\test_gcv_ocr.py %* && echo. && echo ============================================================ && echo Enterキーを押して終了してください... && echo ============================================================ && pause"




